- if f.object.errors.any?
  .errors-global
    %h3
      %b= t('dashboard.transactables.transactable_not_saved')
      = t('dashboard.transactables.address_errors_html', count: f.object.errors.count)

    %ul
      - f.object.errors.full_messages.each do |msg|
        %li= msg
