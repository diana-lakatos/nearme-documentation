- title t('dashboard.nav.edit')

- content_for :page_header do
  %h1= yield :title

  .options
    = link_to 'View Profile', user_path(current_user)


= render :partial => "registrations/form", :locals => { :url => registration_path(resource_name), :country => @country }


/ - content_for :domready do
/   new CategoriesController($('#edit_user'));
/   new EditUserForm()
