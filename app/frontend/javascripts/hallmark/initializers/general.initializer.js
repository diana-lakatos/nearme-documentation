var
  Utils = require('../utils'),
  UI = require('../ui'),
  Onboarding = require('../onboarding'),
  Forms = require('../forms'),
  Fixes = require('../fixes');

Utils.initialize();
UI.initialize();
Onboarding.initialize();
Forms.initialize();
Fixes.initialize();
