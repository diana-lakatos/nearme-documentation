query LocationQuery($location_id: ID!){
  location: location(id: $location_id) {
    name
    description
    description
    availability
    name_and_desc
    company{
      url
      id
      description
    }
  }
}
