%h2 Book space at #{@location.name}


%p.text-center
  = link_to "Log in or sign up", new_user_session_url(:return_to => location_url(@location, :restore_reservations => true)), :class => 'btn btn-green btn-large'
