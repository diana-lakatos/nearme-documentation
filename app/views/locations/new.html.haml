.two-thirds.column
  .box
    %h2 Create a location
    %p.intro
      A location is a physical address, to which you will be able to add bookable
      resources (listings). Once you've created a location, you can add listings to
      it.
    = render :partial => "form"
