%article.company
  %h2
    = location.company.name
    - if location.company.url.present?
      = link_to location.company.url, rel: 'nofollow', target: '_blank' do
        %span.ico-open-link

  %p= location.company.description
  - if location.company.metadata['industries'].any? && platform_context.is_desksnearme?
    .amenities-block
      .amenities-header
        %h3 Industries
        %a{href: '#industries'}
          %h3 Industries
          %span.ico-chevron-right
      .amenities-container
        - location.company.metadata['industries'].in_groups_of(4).each do |industry_group|
          .row-fluid.amenities-row
            - industry_group.compact.each do |industry_name|
              .span3{title: industry_name}
                = industry_name
    .clearfix
