<p>Welcome <%= @email %>!</p>

<p>You can confirm your account email through the link below:</p>

<p><%= link_to 'Confirm my account', confirmation_url(@resource, :confirmation_token => @token, host: PlatformContext.current.domain.try(:name) || ActionMailer::Base.default_url_options[:host]) %></p>
