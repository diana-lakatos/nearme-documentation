%tr
  %td= index + 1
  %td= "#{listing.name} - #{link_to " #{t('instance_admin.manage.approval_requests.preview')}", transactable_type_location_listing_path(listing.transactable_type, listing.location, listing), target: :blank}".html_safe
