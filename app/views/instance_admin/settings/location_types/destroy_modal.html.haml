.modal-header
  %button.close{"data-dismiss" => "modal", :type => "button"}
    %span{"aria-hidden" => "true"} ×
    %span.sr-only Close
  %h4#myModalLabel.modal-title Delete
.modal-body
  %p= "Are you sure you want to delete the '#{@location_type.name}' location type?"

  = simple_form_for @location_type, :url => instance_admin_settings_location_type_path(@location_type), :method => :delete do |f|
    = f.button :submit, 'Delete', class: 'btn btn-danger pull-right'

.modal-footer
  .pull-right
    %button.btn.btn-default{"data-dismiss" => "modal", :type => "button"} Close
