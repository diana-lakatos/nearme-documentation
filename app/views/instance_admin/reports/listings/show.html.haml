.content-container
  .row
    .table-container
      %table
        %tr
          %th
            = t('instance_admin.reports.attribute')
          %th
            = t('instance_admin.reports.value')
        - @transactable.attribute_names.each do |attribute_name|
          %tr
            %td
              = attribute_name
            %td
              - if attribute_name == 'properties'
                = @transactable.properties.to_h.to_s
              - else
                = @transactable.send(attribute_name)

