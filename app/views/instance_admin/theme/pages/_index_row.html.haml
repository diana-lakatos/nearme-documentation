%tr
  %td= page.path
  %td= link_to pages_path(page), pages_path(page), target: '_blank'
  %td
    .pull-right
      = link_to edit_instance_admin_theme_page_path(page) do
        .fa.fa-pencil.fa-2x.inline-form-icon
      = link_to instance_admin_theme_page_path(page), :method => :delete, :confirm => "Are you sure you want to delete this?" do
        .fa.fa-trash-o.fa-2x.inline-form-icon
