.content-container
  %h4 Edit settings
  - if @blog_instance.present?
    = simple_form_for @blog_instance, url: instance_admin_manage_blog_settings_path, html: { multipart: true } do |f|
      = render "form", f: f
  - else
    %p Blog is not available.

