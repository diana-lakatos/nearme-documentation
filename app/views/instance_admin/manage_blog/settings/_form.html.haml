.content-container.dropshadow
  .row
    .input-container.col-xs-6
      = f.input :enabled

    .input-container.col-xs-6
      = f.simple_fields_for :owner do |i|
        = i.input :user_blogs_enabled, label: 'Enable user blogs'
  .row
    .input-container.col-xs-6
      = f.input :name, placeholder: 'Enter blog name', input_html: { class: 'form-control' }
    .input-container.col-xs-6
      = f.input :header, input_html: { class: 'form-control' }

  - if @blog_instance.header.present?
    .row
      .input-container.col-xs-12
        = link_to(@blog_instance.header.filename, @blog_instance.header.url)

  .row
    .input-container.col-xs-12
      = f.input :facebook_app_id, placeholder: 'Enter facebook app ID', input_html: { class: 'form-control' }

  .row
    .input-container.col-xs-6
      = f.input :header_text, placeholder: 'Enter blog header text', input_html: { class: 'form-control' }
    .input-container.col-xs-6
      = f.input :header_motto, placeholder: 'Enter blog motto', input_html: { class: 'form-control' }

  .row
    .input-container.col-xs-12
      = f.input :header_logo, input_html: { class: 'form-control' }

  - if @blog_instance.header_logo.present?
    .row
      .input-container.col-xs-12
        = link_to(@blog_instance.header_logo.filename, @blog_instance.header_logo.url)

  .row
    .input-container.col-xs-12
      = f.input :header_icon, input_html: { class: 'form-control' }

  - if @blog_instance.header_icon.present?
    .row
      .input-container.col-xs-12
        = link_to(@blog_instance.header_icon.filename, @blog_instance.header_icon.url)

.content-container
  .row
    = f.button :submit, "Save", class: 'btn btn-green pull-right'
