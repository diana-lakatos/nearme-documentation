- title t("manage.buy_sell.stock_locations.edit_title")

= show_manage_navigation(:configuration, :stock_locations)

.row-fluid
  .span8.offset2
    .box
      %ul.location-and-listings
        %li.location
          .name.left
            =t("manage.buy_sell.stock_locations.edit_header")

      = simple_form_for [:manage, :buy_sell, @stock_location], :html => {multipart: true, class: "form-horizontal form-expandable" } do |f|

        = render 'form', f: f

        .actions
          = f.submit "Save"
