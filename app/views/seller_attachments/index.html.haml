- if @attachments.count > 0
  %article{id: "location-#{params[:seller_attachable_type].downcase}-documents", class: "location listing tab-pane"}
    - @attachments.each do |attachment|
      %p= link_to attachment.title, seller_attachment_path(attachment)
