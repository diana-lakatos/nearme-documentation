%h1.error-number 500
%h2.semi-bold Oops! Something went wrong. We're on it!
%p It looks as though we've broken something on our system. We've been notified, and will fix it as soon as possible.

= render :partial => 'links', :locals => { :status_code => @status_code }
