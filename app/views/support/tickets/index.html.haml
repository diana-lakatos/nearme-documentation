.row-fluid.support
  .span8.offset2
    %h1= t('support.user.tickets')

    - if @tickets.length == 0
      %p.empty-resultset= t('support.user.no_tickets')
    - else
      .box
        .tickets.list
          = render @tickets

        = will_paginate @tickets
