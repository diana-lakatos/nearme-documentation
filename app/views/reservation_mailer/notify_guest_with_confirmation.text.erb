Your request to book '<%= @listing.name %>' at <%= link_to(@listing.location.name, location_url(@listing.location)) %> has been sent to the host.

Bookings at this workspace require confirmation. Hosts have 24 hours to confirm, but most reply sooner.

<%= render :partial => "shared/mailers/booking_details", :locals => { :listing => @listing, :reservation => @reservation } -%>

You can <%= link_to 'review your travel plans on the Dashboard', bookings_dashboard_url %>.
