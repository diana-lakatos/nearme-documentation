Dear <%= @user.name %>,

<%= @reservation.owner %> has cancelled their booking at <%= @listing %> that they made earlier.

<%= render :partial => "shared/mailers/booking_details", :locals => { :listing => @listing, :reservation => @reservation } %>
This is simply to notify you that the user will not be arriving at your space for this day and time. We’ll re-list your space as available.