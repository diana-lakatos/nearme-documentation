Dear <%= @user %>,

You have made a reservation for <%= @listing %> on <%= l(@reservation.date, :format => :long) %>.

Reservations at this location requires confirmation from the owner/manager. You will receive another email if the user confirms or declines your reservation request.

<%= render :partial => "footer" %>
