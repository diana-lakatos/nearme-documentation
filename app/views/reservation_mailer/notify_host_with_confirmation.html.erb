<p><%= @reservation.owner %> has made a booking for '<%= @listing.name %>' at <%= link_to(@listing.location.name, location_url(@listing.location, :listing_id => @listing)) %>.

<%= render :partial => "shared/mailers/booking_details", :locals => { :listing => @listing, :reservation => @reservation, :host => true } %>

<p>The guest has been notified that confirmation is required from you.</p>

<p>Please confirm or decline their booking <%= link_to 'via the Dashboard', @url %>.</p>
