Your booking for '<%= @listing.name %>' at <%= link_to(@listing.location.name, location_url(@listing.location)) %>  has been cancelled by the host.

<%= render :partial => "shared/mailers/booking_details", :locals => { :listing => @listing, :reservation => @reservation } %>

This booking was previously confirmed, but the plans have changed - I'm sure it's nothing personal.

Don't worry - <%= link_to 'find other locations near you', search_url %>!
