<p>Your booking for '<%= @listing.name %>' at <%= link_to(@listing.location.name, location_url(@listing.location, :listing_id => @listing)) %>  has been cancelled by the host.</p>

<%= render :partial => "shared/mailers/booking_details", :locals => { :listing => @listing, :reservation => @reservation } %>

<p>This booking was previously confirmed, but the plans have changed - I'm sure it's nothing personal.</p>

<p>Don't worry - <%= link_to 'find other locations near you', search_url %>!</p>
