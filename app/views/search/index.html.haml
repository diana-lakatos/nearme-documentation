.grid_3.alpha
  %h1.tk-adelle Workplaces found near:
.grid_9.omega
  = render :partial => "search/bar", :locals => { :value => @query }
#results.container_12
  - if @listings
    = render :partial => "search/listings"
  - else
    .loading.grid_4
      = image_tag "spinner-red.gif"

:javascript
  Search.init("#{search_path}", resultsFromServer = #{@listings ? "true" : "false"});
