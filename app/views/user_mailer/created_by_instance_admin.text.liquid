Welcome, {{ new_user.first_name }}!

You have been invited by {{ creator.name }} to join {{ platform_context.name }}!
The e-mail that was used as an username is {{ new_user.email }}, you can set the password manually by clicking the big blue button below

Set your password at {{ 'set_password_url' | generate_url_with_user_token: new_user }}
