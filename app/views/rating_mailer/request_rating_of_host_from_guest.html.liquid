<h2>How was it, {{ reservation.owner.first_name }}?</h2>

<p>We can't wait to hear about your experience with '<a href="{{ platform_context.host | append:listing.listing_url }}">{{ listing.name }}</a>'.</p>

<p>Your opinion is very important and it will help other {{ platform_context.lessees }} to find great {{ platform_context.bookable_noun_plural }}. Take a moment and give us your feedback!</p>

<a class="btn" href="{{ platform_context.host | append:reservation.host_rating_reservation_url_with_tracking | append:signature_for_tracking }}">Write a Review</a>

<p>If you'd prefer to talk to us directly, please send an email to <a href="mailto:{{ platform_context.support_email }}">{{ platform_context.support_email }}</a> or call us on {{ platform_context.phone_number }} - we love to talk with our {{ platform_context.lessees }}.</p>
