<p>
  Your booking for '<%= @listing.name %>' at <%= @location.name %> ended yesterday.
</p>

<p>
  Leave a rating and maybe a comment on how this booking went by clicking the following link:
  <%= link_to "Rate '#{@listing.name}' visit at #{@location.name}",
              new_reservation_host_rating_url(@reservation.id) %>
</p>
