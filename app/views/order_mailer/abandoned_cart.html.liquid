{% if platform_context.use_cart? %}
  {% assign checkout_url = 'cart_index_path' | generate_url %}
{% else %}
  {% assign checkout_url = 'order_checkout_path' | generate_url: order.id %}
{% endif %}
<h2>Hello {{ enquirer.first_name }},</h2>


<p>we have noticed that you didn't finish booking of '<a href="{{ listing.listing_url }}">{{ listing.name }}</a>'</p>
<p>If you would like to finish the booking process please <a href="{{ platform_context.host | append: checkout_url }}">click here</a>.
