%h1.tk-adelle
  Managing photos for #{link_to @workplace, @workplace}
%ul#photos.photos
  - @photos.each do |photo|
    - unless photo.new_record?
      %li.grid_2
        = image_tag photo.medium.url, :alt => photo.caption, :width => "100%"
        %p= photo.caption
        = form_for([@workplace, photo], :html => {:method => :delete}) do |f|
          = f.submit "Delete", :class => "rm"
%br{:clear => "both"}/
= render :partial => "new"
