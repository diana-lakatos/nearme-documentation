Hi {{ owner.first_name }},

{{ enquirer.name }} ({{ enquirer.profile_url }}) would like join your project, {{ project.name }} ({{ project.show_url }}).
Check out their profile and see what you think. They might just be what you're looking for.
Approve | Decline ({{ project.edit_url_with_token }}) collaborator
