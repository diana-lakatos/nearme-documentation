%h1 Platform Home

- if @views.nil?
  %p= "Views could not be found."

- else
  = simple_nested_form_for [:admin, Instance.new], url: admin_platform_home_url do |instance_form|

    - @views.each_with_index do |view, index|
      %h4= view.path
      %input{type: 'hidden', name: "instance[instance_views_attributes][#{index}][id]", value: view.id}
      %textarea{name: "instance[instance_views_attributes][#{index}][body]", style: 'width: 768px; height: 500px;'}= view.body

    %br
    %p= instance_form.button :submit, 'Save'

