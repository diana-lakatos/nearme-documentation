.row-fluid
  .controls.controls-row
    %legend.span3 Posts
    = link_to 'New post', new_admin_blog_blog_post_path, class: 'pull-right btn btn-primary'

%br

%table.table.sortable
  %thead
    %tr
      %th Name
      %th Created at
      %th Published at
      %th
  %tbody
    - @blog_posts.each do |blog_post|
      = render 'index_row', blog_post: blog_post
