%tr
  %td= link_to domain.name, [:instance_admin,:settings, domain]
  %td
    = domain.state.to_s.humanize
    .small= domain.error_message if domain.failed?
  %td= domain.dns_name
  %td= domain.redirect_to
  %td= domain.use_as_default ? "Yes" : "No"
  %td= domain.uploaded_sitemap.file.present? ? t('instance_admin.settings.domains.uploaded') : t('instance_admin.settings.domains.auto_generated')
  %td= domain.uploaded_robots_txt.file.present? ? t('instance_admin.settings.domains.uploaded') : t('instance_admin.settings.domains.auto_generated')

  / Make sure to move delete to EDIT view, similiar to how liquid views and pages are done.
  %td
    .pull-right
      - if domain.deletable?
        = link_to instance_admin_settings_domain_path(domain), :method => :delete,
            data: { confirm: 'Are you sure you want to delete this?' }, :title => 'Delete' do
          .fa.fa-trash-o.fa-2x.inline-form-icon

    - if domain.editable?
      = link_to edit_instance_admin_settings_domain_path(domain), :title => 'Edit' do
        .fa.fa-pencil.fa-2x.inline-form-icon.pull-right
