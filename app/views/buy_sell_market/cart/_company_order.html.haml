.row-fluid.header-row
  .span6
    Order from #{order.company_name}
  .span2
    Price
  .span1
    Quantity
  .span1
    Total
  .span2
    Actions

- order.line_items.each do |item|
  = render partial: 'cart_item', locals: {item: item}

.row-fluid.cart-row.no-hover
  .span7
    Total for order (incl. tax):
    = order.display_total
  .span2.offset3
    = link_to t('buy_sell_market.cart.buttons.checkout'), order_checkout_path(order, 'address'), class: 'btn btn-green btn-large'
