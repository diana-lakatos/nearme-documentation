.row
  .span12
    %table.table-simple.cart-table
      %thead
        %tr
          %th{colspan: 3}
          %th
            $#{@cart.total}

        %tr
          %th= link_to 'Continue shopping', root_path(anchor: 'products'), class: 'btn btn-blue'
          %th
          %th
          %th
