.row-fluid
  .span12.box
    %h2= t('buy_sell_market.checkout.labels.delivery')

= simple_form_for @order, url: wizard_path do |f|
  = f.fields_for :shipments do |ship_form|
    .box
      .row-fluid
        .span12.centered
          %h3
            Package from
            = ship_form.object.stock_location.name

      .row-fluid
        .span8 ITEM
        .span2 QTY
        .span2 PRICE

      - ship_form.object.manifest.each do |item|
        .row-fluid
          .summary-line
            .span2= item.line_item.decorate.image
            .span6
              = item.variant.name
            .span2
              = item.quantity
            .span2
              = item.line_item.decorate.price

      %ul.field.radios.shipping-methods
        = ship_form.input :selected_shipping_rate_id, collection: ship_form.object.shipping_rates.decorate,
          label: Spree.t(:shipping_method), include_blank: false

    - # TODO !
    -#.shipment
    -#  - if @differentiator.try(:missing?)
    -#    .shipment.unshippable
    -#      %h3.stock-location= Spree.t(:unshippable_items)
    -#      %table.stock-contents
    -#        %col{style: "width: 10%;"}
    -#        %col{style: "width: 60%;"}
    -#        %col{style: "width: 10%;"}
    -#        %col{style: "width: 20%;"}
    -#
    -#        %thead
    -#          %th
    -#          %th{align: 'left'}= Spree.t(:item)
    -#          %th= Spree.t(:qty)
    -#          %th= Spree.t(:price)
    -#
    -#        %tbody
    -#          - @differentiator.missing.each do |variant, quantity|
    -#            %tr.stock-item
    -#              %td.item-image= mini_image(variant)
    -#              %td.item-name= variant.name
    -#              %td.item-qty= quantity
    -#              %td.item-price= display_price(variant)

  = render partial: 'checkout_buttons', locals: { form: f }
