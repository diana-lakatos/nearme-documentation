.button-row
  - if defined? form
    %button.btn.blue.pull-right.next= t('buy_sell_market.checkout.buttons.next_step')

  - if step == :address
    = link_to cart_index_path do
      %span.btn.gray.back= t 'buy_sell_market.checkout.buttons.previous_step'
  - else
    = link_to previous_wizard_path do
      %span.btn.gray.back= t 'buy_sell_market.checkout.buttons.previous_step'



    -#%span.btn.blue.pull-right.next
    -#  = form.submit
