- content_for :domready do
  - if params[:action] == 'booking_failed'
    $(document).trigger('load:modal.nearme', ['#{booking_failed_listing_reservation_path(@reservation.transactable, @reservation)}']);
