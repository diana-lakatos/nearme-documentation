<h1>Hi {{ owner.first_name }},</h1>

<p><a href="{{ enquirer.profile_url }}">{{ enquirer.name }}</a> would like join your group, <a href="{{ group.show_url }}">{{ group.name }}</a>.
<p>Check out their profile and see what you think. They might just be what you're looking for.</p>
<p><a href="{{ group.edit_url_with_token }}">Approve | Decline group member</a></p>
