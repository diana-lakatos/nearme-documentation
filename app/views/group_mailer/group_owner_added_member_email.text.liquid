Hi {{ user.first_name }},

{{ owner.name }} ({{ owner.profile_url }}) has invited you to become a member on {{ group.name }} ({{ group.show_url }}).
Learn more about the group.

Accept | Decline invitation ({{ user.groups_profile_url_with_token }})
