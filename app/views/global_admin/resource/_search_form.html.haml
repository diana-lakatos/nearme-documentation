= form_tag '', :method => :get, :class => 'form-search' do
  = text_field_tag 'search', params[:search], :class => 'search-query', :placeholder => "Type a name or email address"
  = submit_tag 'Search', :class => 'btn'

