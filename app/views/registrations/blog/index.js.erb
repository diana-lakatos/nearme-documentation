$('#user-blog-posts').append('<%= j render partial: 'registrations/blog/blog_post', collection: @blog_posts %>');

<% if @blog_posts.next_page %>
  $('.pagination').replaceWith('<%= j will_paginate @blog_posts %>');
<% else %>
  $(window).off('scroll');
  $('#spinner').remove();
<% end %>
