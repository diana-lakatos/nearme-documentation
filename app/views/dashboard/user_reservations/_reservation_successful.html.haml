%h4.reservation-emphasis
  = t('reservations.booking_was_requested_successfully')

.reservation-list.just-booked
  = render 'reservation', reservation: @reservation.decorate

- unless (@reservations - [@reservation]).empty?
  %h4.other-reservation-separator
    #{pluralize((@reservations - [@reservation]).count, ' other booking')}
