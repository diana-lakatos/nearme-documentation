.box
  - if @visits.total_entries.zero?
    %p.empty-resultset
      = t('dashboard.analytics.no_location_visits_yet')
  - else
    .chart
      .total.right
        .total-number.left
          %span.total
            = "#{@chart.total} visits"
        %span{class: 'total-text'} 30 day total
      %canvas#chart

    %table.table.table-simple
      %thead
        %tr
          %th Date
          %th Count
      %tbody
      - @visits.each do |visit|
        %tr
          %td
            = visit.impression_date
          %td
            = visit.impressions_count

    = will_paginate_styled @visits, :params => { :analytics_mode => 'location_views' }
