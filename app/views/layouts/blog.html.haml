!!!
%html
  %head
    %title= blog_title_tag
    %meta{:'http-equiv' => "Content-type", :content=> "text/html;charset=UTF-8"}
    %meta{content: "width=device-width, initial-scale=1, minimum-scale=1, maximum-scale=1", name: "viewport"}/
    %meta{content: "Near Me is a complete solution for setting up and managing your own marketplace. No coding required.", name: "description"}/
    = csrf_meta_tag
    = render 'layouts/favicon'
    /[if lt IE 9]
      = stylesheet_link_tag 'browser_specific/ie8'
    = stylesheet_link_tag '//cdnjs.cloudflare.com/ajax/libs/twitter-bootstrap/3.0.3/css/bootstrap.min.css'
    = stylesheet_link_tag 'blog'
    = stylesheet_link_tag platform_context.compiled_stylesheet_url
    = javascript_include_tag :blog

  %body.blog
    - unless @blog_instance.is_near_me?
      .container-fluid.header-container
        = render 'layouts/navbar'
    .header
      - header = @blog_post.try(:header).presence || @blog_instance.header
      - if header.present?
        .header-image
          = link_to blog_posts_path do
            = image_tag header.url, title: @blog_instance.name
      - else
        .heading
          = link_to @blog_instance.name, blog_posts_path

    .container-fluid
      .col-md-10.col-md-offset-1.col-lg-8.col-lg-offset-2
        = yield

    = yield :script

    = render 'layouts/google_analytics'
