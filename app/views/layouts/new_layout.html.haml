!!!
%html
  %head
    %title
      DesksNear.Me :: #{show_title? ? yield(:title) : "Where do you want to work today?"}
    %meta{:content => "DesksNear.Me is a public reservation system for companies to offer desks to freelancers and sole-operators.", :name => "description"}/
    %meta{:content => "The Rad Warlike Annex", :name => "author"}/
    %meta{:content => "tellthemwhen_verif_1af11d7040aa81a9ab2e07d4efee180d2e93d030", :name => "TellThemWhen"}/
    %meta{:content => "IE=edge,chrome=1", "http-equiv" => "X-UA-Compatible"}/
    = csrf_meta_tag
    %link{:href => "/favicon.ico?9002", :rel => "shortcut icon"}/
    = stylesheet_link_tag 'new_layout'
    = stylesheet_link_tag 'vendor/fancybox/jquery.fancybox-1.3.1', :media => "screen, projection"

    = javascript_include_tag :application

    %script{:src => "https://maps.google.com/maps/api/js?sensor=true", :type => "text/javascript"}
  %body
    #TTWNotify{:style => "display:none;"}
    .container
      %header.row
        .eight.columns
          = link_to "Desks Near Me", root_url, :id => "logo"
        #header_links.eight.columns.text-right
          - if user_signed_in?
            = link_to "Dashboard", dashboard_path, :class => "header-link"
            = link_to "Account", edit_user_registration_path, :class => "header-link"
            = link_to "Logout", destroy_user_session_path, :method => :delete, :class => "header-link"
          - else
            = link_to "Sign Up", new_user_registration_url, :class => "header-link"
            = link_to "Log In", new_user_session_url, :class => "header-link"
            = link_to "List Your Space!", new_location_url, :class => "btn btn-blue btn-medium"

    %section#content{:class => "#{controller_name} #{'legacy' if legacy?}"}
      = render "layouts/hero"
      = flashes
      .container
        = yield

    = render :partial => "layouts/footer"

    = yield :script
    = render :partial => "layouts/analytics" if Rails.env.production?

    %script{:charset => "utf-8", :type => "text/javascript"}
      (function(){
      t = document.createElement('script');t.async=true;t.type ='text/javascript';
      t.src = ('https:' == document.location.protocol ? 'https://secure.' : 'http://api.') + 'tellthemwhen.com/api/v2/notices/620b94a22ed.js';
      var s=document.getElementsByTagName('script')[0]; s.parentNode.insertBefore(t,s);
      })();
    = render :partial => "layouts/feedback"
