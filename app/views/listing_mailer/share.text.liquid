Dear {{ name }},

{{ sharer.name }} has shared a listing with you!

View {{ listing.name }} on {{ platform_context.name }} ({{ listing.listing_url }}).
{% if message %}
They also wanted to say:

"{{ message }}"{% endif %}
