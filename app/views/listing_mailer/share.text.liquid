Dear {{ name }},

{{ sharer.name }} has shared a listing with you!

View {{ listing.name }} on {{ platform_context.name }} ({{ platform_context.host | append:listing.location.url }}).
{% if message %}
They also wanted to say:

"{{ message }}"{% endif %}
