- [:success, :warning, :error, :deleted].each do |key|
  - if flash[key].present?
    %div{class: "alert #{key}", 'data-flash-message' => true}
      %p
        %i
        = flash[key].html_safe
        %a.close.ico-close.right{href: '#'}

- if admin_as_user?
  .alert.static.warning{'data-flash-message' => true}
    %p
      %i
        #{original_admin_user.name}, you're currently browsing the website as #{current_user.name}
