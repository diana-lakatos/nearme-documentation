:javascript
  window.__insp = window.__insp || [];
- if Rails.env.production?
  :javascript
    var tags = #{get_and_clear_stored_inspectlet_taggable_events}
    for(i = 0; i < tags.length; i++){
      __insp.push(['tagSession', tags[i]])
    }
    = javascript_include_tag 'analytics/inspectlet'
