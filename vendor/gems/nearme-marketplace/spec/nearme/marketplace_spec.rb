require "spec_helper"

describe Nearme::Marketplace do
  it "has a version number" do
    expect(Nearme::Marketplace::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
