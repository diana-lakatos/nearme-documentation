- html_attributes = attribute.validation_rules.present? ? { maxlength: attribute.validation_rules['length']['maximum'] } : {}
= f.input attribute.name, attribute.options.deep_merge(input_html: { value: f.object.send(attribute.name) }.merge(html_attributes))
