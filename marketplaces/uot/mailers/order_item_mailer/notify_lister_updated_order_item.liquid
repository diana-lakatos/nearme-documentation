Hello, {{ lister.name }}

{{ enquirer.name }} has been working on your project. They have recently updated submitted time and expense items for your approval.
{% assign order_item_url = 'dashboard_company_order_order_items_path' | generate_url: @order_item.order.id, transactable_id: @order_item.order.transactable.id %}
Click here to review time and expense details.

{% if @order_item.approve_at  %}
  Please note that those expenses are going to be approved automatically at {{ @order_item.approve_at | localize }} if no action is taken.
{% endif %}
