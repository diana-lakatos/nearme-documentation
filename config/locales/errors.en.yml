en:
  activemodel:
    errors:
      models:
        reservation_request:
          attributes:
            base:
              file_cannot_be_empty: "File cannot be empty"
        product_form:
          attributes:
            base:
              label_empty: "Document requirements label can't be blank"
              description_empty: "Document requirements description can't be blank"
  activerecord:
    attributes:
      listing_type:
        name: "Listing type"
      location_type:
        name: "Location type"
    errors:
      models:
        recurring_booking:
          one_valid_reservation: 'The selected dates are not available, please choose different dates.'
        instance:
          attributes:
            pricing_options:
              must_be_selected: "At least one of the pricing options must be selected."
          test_mode_needs_password: 'is needed for test mode.'
        location_type:
          attributes:
            name:
              taken: "already exists."
        instance_view:
          attributes:
            service_type:
              not_accessible: "can't associate Service Type with this template, because this template is used by the following workflow alerts, which are irrelevant to Service Type: %{workflow_alerts}"
        workflow_alert:
          attributes:
            template_path:
              not_accessible: 'this template path is associated with Service Type, but Service Type is irrelevant to this workflow alert. Please choose a different template or de-associate Service Type from the chosen one.'
        user:
          attributes:
            email:
              taken: "That email address is associated with an existing account."
            name:
              first_name_too_long: "first name is too long (maximum is %{count} characters)"
              middle_name_too_long: "middle name is too long (maximum is %{count} characters)"
              last_name_too_long: "last name is too long (maximum is %{count} characters)"
        transactable:
          attributes:
            free: "must be free if no prices are provided"
            book_it_out_minimum_qty: "Must be less than or equal to quantity"
            cant_set_montly_price: "Cant't set monthly price if listing is not open 7 days a week."
            no_consecutive_days: "Booking will not be possible if no consecutive days are open."
        spree/shipping_method:
          attributes:
            base:
              zone_incomplete: 'Shipping profile is incomplete'
        spree/zone:
          attributes:
            kind:
              members_missing: 'List of shipping destinations cannot be blank'
        review:
          attributes:
            rating:
              rating_is_required: 'Rating is required'
        project:
          attributes:
            photos:
              too_short: 'You need to add at least one photo to your project'
            topics:
              too_short: 'You need to select at least one topic for your project'
  errors:
    messages:
      free: "must be free if no prices are provided"
      free_if_zero: "must be free if all prices are zero"
      price_cant_be_blank: "can't be blank"
      wrong_content_type: "is the wrong content type"
      wrong_size: "is the wrong size (should be %{file_size})"
      size_too_small: "is too small (should be at least %{file_size})"
      size_too_big: "is too big (should be at most %{file_size})"
      cant_review_own_product: "can't review own product"
      onboard_issue: "Onboarding failed, the error message was: %{error}"
      inaccurate_address: 'Please provide a more accurate address'
      must_be_later: 'must occur after beginning'
      not_in_past: 'must be not in past'
      range_too_wide: "can 't be later than 1 year from starting date"

